import { TextField } from "@shopify/polaris";

const SearchCol = () => {
  return (
    <div className="section search-col-wrapper">
      <h3>the shoppies</h3>
      <h1>Nominate</h1>
      <TextField label="Search movies" value="hello" onChange={() => {}} />
    </div>
  );
};

export default SearchCol;
